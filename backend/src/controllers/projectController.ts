import { Request, Response } from 'express';

export const handleCreateProject = (req: Request, res: Response): void => {
  console.log(req, res);
}

export const handleGetProject = (req: Request, res: Response): void => {
  console.log(req, res);
}

export const handleDeleteProject = (req: Request, res: Response): void => {
  console.log(req, res);
}

export const handleUpdateProject = (req: Request, res: Response): void => {
  console.log(req, res);
}


import { user, project, url_mapping, oauth_token } from "@prisma/client";

export type User = user;
export type Project = project;
export type UrlMapping = url_mapping;
export type OauthToken = oauth_token;